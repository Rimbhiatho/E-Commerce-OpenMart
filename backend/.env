# OpenMart POS Backend Configuration
# Environment variables - do not commit to version control

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=openmart-secret-key-change-in-production-at-least-32-characters-long
JWT_EXPIRES_IN=8h

# Database Configuration (SQLite)
DB_PATH=./openmart.db

